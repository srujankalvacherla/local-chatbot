1. chromadb - for vector storage
2. sentence-transformers/all-MiniLM-L6-v2 - from Hugging face for - maps sentences to vectors 
3. PyPDFLoader — reads a PDF file and converts each page into a LangChain Document object
4. DirectoryLoader — scans a folder and loads all matching files (e.g. all *.txt or *.pdf files)