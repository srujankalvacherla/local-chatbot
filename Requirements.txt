# ── HTTP & environment ────────────────────────────────
openai>=1.0.0
python-dotenv>=1.0.0

# ── Terminal UI ───────────────────────────────────────
rich>=13.0.0

# ── Web UI ────────────────────────────────────────────
streamlit>=1.35.0

# ── LangChain core ────────────────────────────────────
langchain-core>=0.2.0
langchain>=0.2.0
langchain-ollama>=0.1.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-classic>=0.0.1

# ── Embeddings (moved out of langchain-community) ─────
langchain-huggingface>=0.0.3

# ── Vector store (moved out of langchain-community) ───
langchain-chroma>=0.1.0

# ── Vector store backend ──────────────────────────────
chromadb>=0.5.0

# ── Embeddings model ──────────────────────────────────
sentence-transformers>=3.0.0

# ── Document loaders ──────────────────────────────────
pypdf>=4.0.0

# ── PyTorch (required by sentence-transformers) ───────
torch>=2.0.0

# ── LangGraph (replaces RunnableWithMessageHistory) ───
langgraph>=0.2.0